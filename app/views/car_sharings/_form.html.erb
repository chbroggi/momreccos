<%= form_for(car_sharing) do |f| %>
  <% if car_sharing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_sharing.errors.count, "error") %> prohibited this car_sharing from being saved:</h2>

      <ul>
      <% car_sharing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :nb_seats %>
    <%= f.number_field :nb_seats %>
  </div>

  <div class="field">
    <%= f.label :event_id %>
    <%= f.text_field :event_id %>
  </div>

  <div class="field">
    <%= f.label :player_id %>
    <%= f.text_field :player_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
